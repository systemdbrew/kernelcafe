apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: edge-vips
  namespace: metallb-system
spec:
  # Documentation-only VIPs. Production addresses are intentionally omitted.
  addresses:
    - 10.10.90.10/32
    - 10.10.90.11/32
